name: con-name
run-name: "Name: ${{ github.event.pull_request.title || github.event.head_commit.message }}"

on: 
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  validation:
   name: validation
   uses: nasir17git/called/.github/workflows/validation.yaml@main
   secrets: inherit
  calling:
   if: false
   name: build
   uses: nasir17git/called/.github/workflows/con-name.yaml@main
   secrets: inherit
