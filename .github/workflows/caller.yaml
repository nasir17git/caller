name: Caller Workflow

on:
  push:
  workflow_dispatch:

jobs:
   calling:
    name: build
    uses: nasir17git/called/.github/workflows/called.yaml@main
    with:
      username: 230727
    secrets: inherit
