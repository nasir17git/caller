name: Caller Workflow

on:
  push:
  workflow_dispatch:

jobs:
   calling:
    name: build
    uses: nasir17git/called/.github/workflows/called.yaml@main
    with:
      username: 230727
      choice: false
      sentence: wgwegwe-fff
    secrets: inherit
