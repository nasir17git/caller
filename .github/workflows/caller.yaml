name: Caller Workflow

on:
  workflow_dispatch:

jobs:
   calling:
    name: build
    uses: nasir17git/called/.github/workflows/called.yaml@main
    with:
      username: 230727
    secrets: inherit
