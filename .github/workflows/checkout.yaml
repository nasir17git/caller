name: checkout
run-name: "checkout"

on: 
  push:
  workflow_dispatch:

jobs:
  where:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: main
      - name: check
        run: |
          git branch

